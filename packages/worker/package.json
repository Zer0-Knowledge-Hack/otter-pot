{
    "name": "@ss/worker",
    "version": "0.1.0",
    "private": true,
    "description": "Bot de Telegram + orquestador entre la Mini App y ChallengePool — Cloudflare Workers",
    "main": "src/index.ts",
    "scripts": {
        "dev": "wrangler dev --local",
        "deploy": "wrangler deploy",
        "lint": "eslint src test --ext .ts",
        "lint:fix": "eslint src test --ext .ts --fix",
        "test": "vitest run",
        "check-types": "tsc --noEmit"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20250801.0",
        "@typescript-eslint/eslint-plugin": "^6.0.0",
        "@typescript-eslint/parser": "^6.0.0",
        "eslint": "^8.0.0",
        "typescript": "^5.3.0",
        "vitest": "^1.6.0",
        "wrangler": "^3.78.0"
    },
    "dependencies": {
        "viem": "2.39.0"
    }
}
